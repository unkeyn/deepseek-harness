/** Host loader entry for the browser-only Freebuff OAuth settings tab. */

/** This plugin has no host-side behavior; its implementation is exported from `./client`. */
export function apply(): void {}
