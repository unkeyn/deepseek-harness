/**
 * Compatibility-only package root for fork packages written before the
 * upstream client-runtime split. It deliberately registers no Cordis plugin.
 */
export const name = 'fork-client-runtime-compat'
