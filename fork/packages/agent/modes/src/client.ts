/**
 * Client-namespace projection of the agent-mode domain: the
 * `SessionProjectionMap.agentMode` merge and the roster types, served to
 * browser aggregates without importing any host-side value.
 * @module @deepseek-ai/dsh-fork-agent-modes/client
 */

export type * from './types.ts'
