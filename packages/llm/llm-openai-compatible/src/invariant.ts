/** No runtime invariant: the provider package is currently a dormant skeleton. */

import type { Context } from '@deepseek-ai/cordis'

export const name = 'llm-openai-compatible'

/**
 * Install no invariant until the provider publishes runtime state.
 * @param _ctx - invariant registry context.
 */
export function apply(_ctx: Context): void {}
